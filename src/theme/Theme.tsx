"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFCC00",
      light: "#8F8F8F",
    },
    secondary: {
      main: "#034AB2",
    },
  },
});

export default theme;